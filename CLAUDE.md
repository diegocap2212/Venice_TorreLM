# Venice BP Hub - Handover & Context

## Current Status (2026-04-29)
- **Git State:** Project recently updated from remote `master` branch.
- **Design:** Migrated to **Venice Premium 2.0** (Emerald/Slate theme).
- **Architecture:** Moved to Next.js route groups `(dashboard)`.
- **Auth:** NextAuth v5 integrated with email whitelist in `src/auth.ts`.

## Technical Blocker: Prisma Error P1013
- The `schema.prisma` is configured for `postgresql`.
- The local `.env` is pointing to `sqlite` (`file:./prisma/dev.db`).
- **Action Required:** Update `.env` with a PostgreSQL connection string OR revert schema to `sqlite`.
- **Note:** `src/lib/prisma.ts` currently uses `@prisma/adapter-pg`.

## Pending Requirements (Validated by Antigravity)
1. **Candidates CPF:** Logic is in `src/lib/cpf-utils.ts` (Hashing/Masking). Need to check "new tab" from spreadsheet for extra fields.
2. **Termination Date:** `data_desligamento` added to `Colaborador` model but needs better UI visibility in the table/drawer.
3. **Evaluations Space:** `PerformanceDashboard` has slots for Auto-evaluation/PDI/PIP. Need to define if more specific forms are required.
4. **Performance Logic:** Ensure `CicloPerformance` uniqueness for `(colaborador_id, periodo, tipo)` to avoid duplicates.
5. **Data Completeness:** Map and add remaining fields from the spreadsheet (HR specific data like contract type, costs, etc.).
6. **Privacy (Hidden Fields):** 
   - Add `salario` and other sensitive fields to `Colaborador`.
   - Implement a "toggle visibility" (Eye icon) in the UI for these fields.

## Tech Stack
- Next.js 15+ (App Router)
- Prisma (PostgreSQL intended)
- NextAuth v5 (Beta)
- Tailwind CSS 4
- Lucide Icons
- shadcn/ui

---
*Context generated by Antigravity AI for Claude Code handover.*
